[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "easyedit"
version = "0.0.0"
description = "Local editable install of EasyEdit"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["easyeditor*"]
