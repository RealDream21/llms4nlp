# We used uv as a packet manager
## Install the packages using uv sync
## You can generate datasets starting from the counterfact(azhx/counterfact) dataset using the dataset_generation.py script (the command is uv run dataset_generation.py). You will also need to run a judge model on ollama. We used gemma4:e4b 

